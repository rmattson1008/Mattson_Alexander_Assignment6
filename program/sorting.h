#include "ItemType.h"


class Sorting {
int SelectionSort(ItemType[] unsorted);
int MergeSort(ItemType[] unsorted);
int QuickSort(ItemType[] unsorted);
int HeapSort(ItemType[] unsorted);
}

// Sorting::SelectionSort(raw data)